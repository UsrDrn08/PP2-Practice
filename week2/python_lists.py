# Создание списка
numbers = [1, 2, 3, 4]

# Доступ по индексу
print(numbers[0])

# Изменение элемента
numbers[1] = 10

# Добавление элемента
numbers.append(5)

# Перебор списка
for n in numbers:
    print(n)
